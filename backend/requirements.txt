# Core framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.30
alembic>=1.13.0
aiosqlite>=0.20.0

# Data & Validation
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# Market Data (all optional — lazy-loaded if import fails)
alpaca-py>=0.28.0
yfinance>=0.2.37
ccxt>=4.3.0
aiohttp>=3.9.0
websockets>=12.0
pandas>=2.2.0
numpy>=1.26.0
httpx>=0.27.0

# Technical Analysis
# NOTE: pandas-ta depends on numba which does NOT support Python 3.14 yet.
# We use pure-Python indicator calculations in app/data/indicators.py instead.
# pandas-ta>=0.3.14b0

# ML & Optimization (Optional — install when Python 3.14 support arrives)
# scikit-learn>=1.5.0
# optuna>=3.6.0

# Utils
orjson>=3.10.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
truststore>=0.10
